version: chrona/presentation/v0.1
kind: view
id: controller-risk-review
body:
  selection: {include: {types: [span, point]}}
  grouping: {by: objectType, missing: ungrouped}
  ordering: {by: plannedStart, direction: ascending, tieBreak: id}
  window: {mode: selected-planned, marginDays: 7}
  comparison: {baseline: primary, actual: optional, observationSelection: latest, deltaUnit: calendar-days, facets: [planned, actual, missingActual]}
  visibility: {labels: true, relations: semantic, annotations: all}
  layoutIntent: {compactness: balanced, itemStacking: stable}
  annotations:
    - id: supplier-risk
      purpose: callout
      anchor: {kind: object, id: firmware, endpoint: body}
      placement: {side: above, alignment: end}
      text: Supplier confirmation pending
