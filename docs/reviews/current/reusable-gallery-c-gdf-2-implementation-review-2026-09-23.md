# C-GDF-2 Paired Ordinary Corpus Gallery Implementation Review

**Decision:** Accepted

Controller Z now declares Plan-only as a second immutable Context and public
materializer SVG. Its Project and Actual references equal Executive's; its View
reference differs while Theme, Scheme, Layout, environment, and target remain
ordinary declared resources. The new gallery catalogue validator proves those
facts, target capability claims, pair completeness, and documentary fields
without reaching the render pipeline.

The flat index is replaced rather than retained. The initial gallery contains
only this meaningful pair; unrelated existing corpus slides remain regression
evidence until they have a paired comparison declaration. Focused materializer,
preset-evidence, and catalogue tests pass. The generated Plan-only SVG was
produced by the public materializer.
