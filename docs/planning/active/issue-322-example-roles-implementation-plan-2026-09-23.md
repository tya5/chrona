# #322 Example Roles Implementation Plan

1. Add explicit regression-corpus role and per-slide evidence text to each
   manifest.  Extend the public materializer's manifest validation without
   changing Context closure or generated bytes.
2. Add an inventory gate with focused malformed metadata and dangling-link
   fixtures.  It reports live schema files, corpus slides, curriculum links,
   and gallery links deterministically; wire it into conformance.
3. Add one curriculum index and one gallery index that link only to declared
   corpus resources and commands.  Add documentation tests for those links.
4. Run focused tests, conformance, full pytest, every public materializer,
   generated-SVG diff, wheel smoke, and publish acceptance before closing #322.
