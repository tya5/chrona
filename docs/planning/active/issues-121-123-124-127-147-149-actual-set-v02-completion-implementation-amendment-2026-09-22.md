# Actual Set v0.2 Completion Implementation Amendment

Add optional Date-only `body.asOf` to the v0.2 schema.  Migrate examples,
conformance fixtures, and tests with deterministic provenance identities for
external observations; delete v0.1 schema/parser/inventory routes; prove
render-context materialization, closure-input, command, conformance, and
schema-inventory behavior before resuming P7 final review.
