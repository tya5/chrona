# Issue 125 Presentation Flexibility Implementation Plan

1. Publish View v0.4 and typed contract migration, deleting v0.3 acceptance.
   Add schema tests for bounded intent, ladders, inheritance, and rejection.
2. Add Layout fallback resolution and typed manifest evidence.  Test stable rung
   choice, suppression, diagnostic exhaustion, and no Scene retry.
3. Add validated profile-package presentation extension records and collision
   checks, then connect namespaced column/semantic facts through the existing
   typed vocabulary path.
4. Migrate examples, add public materializer/property checks for selected-rung
   evidence, run focused tests and the full suite, review generated SVG diffs,
   and publish as one atomic PR.

Acceptance requires one live View version, deterministic manifest evidence,
unchanged renderer responsibility, extension collision rejection, and all
public examples materializing without a policy regression.
