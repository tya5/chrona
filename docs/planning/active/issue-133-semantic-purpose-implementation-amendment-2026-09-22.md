# Issue 133 Semantic-purpose Implementation Amendment

Before the v0.5 critical relation surface resumes, change the registry acceptance invariant to unique `(purpose, scene_role)` pairs, retain unique semantic IDs, and add the explicit normal/critical dependency variant test. Publish and merge this correction independently; then restore the deferred surface implementation and re-run its full gate.
