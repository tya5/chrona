# Implementation Amendment — Shared-Obstacle Connector Topology (#466 O2)

**Plan amended:** [O1–O3 implementation plan](issue-466-shared-obstacle-prerequisite-implementation-plan-2026-09-26.md). **Design:** [topology correction](../../design/issue-466-annotation-connector-topology-correction-2026-09-26.md) and [architecture review](../../reviews/current/issue-466-annotation-connector-topology-architecture-review-2026-09-26.md). **Public product base:** O1 only; the dirty O2 worktree is not accepted evidence. The seven literal acceptance rows remain in the parent plan. This amendment addresses row 1 and the O2 prerequisite only, not rows 2–6.

| Slice | Owners and migration | Focused evidence and gate | Publication |
| --- | --- | --- | --- |
| O2a inventory phase closure | `surface_composer.py`, `labels.py`, `routing.py`, `annotations.py`, `ports.py`, obstacle/label/port tests | Exactly one index, route-first optional-label phase, all marks/text/rules/dependencies registered, named host/port exemptions only, neutral note-beside-dependency box test. No candidate schema change. | Commit/push after batch Scene/SVG comparison and CI. |
| O2b atomic rail box/connector trial | New Layout annotation candidate/search helper; composer calls it; `surface_quality.py` decision fields | Candidate box and full leader route are tested before mutation; failed pair leaves inventory unchanged; bounded port/box/topology trials and selected decision. Controller-z leaders remain local, no lost dependency or note index. | Commit/push after rendered controller-z and HALCYON batch review and CI. |
| O2c bridge projection | Layout path-command construction, `RelationPlacement`, `scene/v05_builder.py`, SVG/typeset parity tests and generated public evidence | Typed transverse crossings only; later leader gap, earlier/dependency stroke continuous; identity and marker preserved; no mark/text/rule crossing; no perimeter route; explicit fallback diagnostic. | Commit/push with reviewed generated bytes and CI. |
| O3 acceptance review | `docs/reviews/current/` | Row 1 direct fixture and all public materializers; full three-OS CI, wheel/smoke and newest-Python evidence as specified; rows 2–6 explicitly not met. | Publish review; keep #466 open. |

Before each product slice, discard or selectively adapt the unaccepted local experiment only after diff review; do not publish its stale generated files. Run focused tests locally in `.venv311`; run public materializers and inspect Scene/SVG/raster changes as a batch. Let CI supply full pytest. Fetch `origin/main`, inspect exact staged changes and ahead/behind state, push serially, and verify the remote commit. A new policy or schema gap pauses code and returns to design/review/plan publication.
