# Implementation Plan — Committed Capability Slides (#434)

- **I434-1:** `tools/presentation_coverage.py` (non-default integers), `tools/corpus_coverage.py` (unreferenced files with reasons, `--check`), regenerated reports, and unit tests in `tests/unit/tools/`.
- **I434-2:**
  - the Controller Z `executive-light` leader treatment;
  - new `views/capabilities.yaml`, `layouts/capabilities.yaml`, `themes/capabilities.yaml`, `contexts/capabilities.yaml` and a manifest entry;
  - generated evidence, refreshed reports and updated corpus counts;
  - `tests/integration/test_capability_slides.py`.
- **I434-3:** the literal acceptance review, after CI.
