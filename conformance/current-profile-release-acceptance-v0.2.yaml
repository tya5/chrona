version: chrona/release-acceptance/v0.2
releaseId: current-profile-svg-v0.1
inputClosure:
  projectIdentity: project:release-fixture
  revisionIdentity: revision:release-fixture-v1
  packageClosureIdentity: packages:release-fixture-v1
  viewIdentity: view:engineering
  actualSetIdentity: actuals:release-fixture-v1
  sceneIdentity: scene:release-fixture-v1
  evaluationIdentity: evaluation:release-fixture-v1
output: {target: svg, targetVersion: chrona/output-capability/v0.2}
useCases:
  - {id: UC-01, disposition: accepted, evidence: [tests/unit/chrona/scheduling/test_scheduler.py, tests/integration/test_conformance.py]}
  - {id: UC-02, disposition: accepted, evidence: [tests/cli/test_cli.py, tests/unit/chrona/storage/test_snapshots.py]}
  - {id: UC-03, disposition: accepted, evidence: [tests/unit/chrona/presentation/model/test_actual_intake.py, tests/unit/chrona/presentation/model/test_presentation_closure.py]}
  - {id: UC-04, disposition: accepted, evidence: [tests/unit/chrona/presentation/model/test_presentation_closure.py, tests/integration/test_render.py]}
  - {id: UC-05, disposition: excluded, reason: Interactive editor behavior has library evidence but no user-facing adapter.}
  - {id: UC-06, disposition: excluded, reason: AI proposal behavior has library evidence but no user-facing adapter.}
  - {id: UC-07, disposition: excluded, reason: Profile validation is partial and package acquisition/profile UX is not exposed.}
  - {id: UC-08, disposition: excluded, reason: Annotation commands and Scene behavior are not exposed by a user-facing editor.}
  - {id: UC-09, disposition: accepted, evidence: [tests/unit/chrona/presentation/model/test_presentation_closure.py, tests/unit/chrona/app/test_interactive.py]}
  - {id: UC-10, disposition: excluded, reason: Actual rendering exists but external intake and reconciliation are not exposed.}
  - {id: UC-11, disposition: excluded, reason: The CLI exposes a subset but not the revision-bound automation and Command workflow required by the full use case.}
  - {id: UC-12, disposition: excluded, reason: Immutable comparison exists but no named-baseline capture command is exposed.}
  - {id: UC-13, disposition: accepted, evidence: [tests/unit/chrona/release/test_output.py, tests/integration/test_render.py]}
  - {id: UC-14, disposition: excluded, reason: Federation remains a library-only successor and is not consumed by render-review.}
  - {id: UC-15, disposition: excluded, reason: Delivery-profile library fixtures exist but profile acquisition and roadmap UX are not exposed.}
