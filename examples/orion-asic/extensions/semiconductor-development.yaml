# Semiconductor development profile package: gate profiles and typed fields for an ASIC programme.
# contentIdentity frames this document with the contentIdentity field omitted (Specification 62 correction).
version: chrona/profile/v0.3
packageId: semiconductor-development
contentIdentity: sha256:e0571fd30fc81b41a304f824f8a81ba8c739b9208f96d03310ca6eac02733493
requires:
  projectFormat: timeline/v0.7
profiles:
  EVT:
    extends: milestone
    fields:
      approval:
        type: enum
        enumValues:
        - pending
        - approved
        - rejected
        required: true
  DVT:
    extends: milestone
    fields:
      approval:
        type: enum
        enumValues:
        - pending
        - approved
        - rejected
        required: true
  PVT:
    extends: milestone
    fields:
      approval:
        type: enum
        enumValues:
        - pending
        - approved
        - rejected
        required: true
fields:
  revision:
    type: string
    required: true
    cardinality: one
  customerReview:
    type: boolean
    cardinality: one
  lot:
    type: string
    cardinality: many
